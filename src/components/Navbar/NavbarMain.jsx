const NavbarMain = () => {
  return (
    <nav className="absolute">
      <a href="#">room</a>
      <div>
        <a href="#">home</a>
        <a href="#">shop</a>
        <a href="#">about</a>
        <a href="#">contact</a>
      </div>
    </nav>
  );
};

export default NavbarMain;
